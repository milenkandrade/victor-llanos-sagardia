<nav class="fixed top-5 left-10 right-10 z-50 bg-white/60 backdrop-blur-grayscale rounded-full shadow-sm">
    <div class="max-w-screen-xl mx-auto p-3">
      <div class="flex items-center justify-between">
        <!-- Logo -->
        <div class="flex items-center ">
          <img src="mandolina.png" alt="Logo" class="h-16">
          <span class="text-lg text-gray-600 font-semibold">Victor Llanos Sagardia</span>
        </div>
  
        <!-- Menú principal -->
        <div class="hidden md:flex items-center gap-6">
          <a href="#" class="text-gray-900 text-xl hover:text-gray-400">Biografia</a>
          <a href="#" class="text-gray-900 text-xl hover:text-gray-400">Proyectos</a>
          <a href="#" class="text-gray-900 text-xl hover:text-gray-400">Galeria</a>
        </div>
  
        <!-- Botón móvil -->
        <button class="md:hidden">
          <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>
    </div>
  </nav>
  


  <!-- <nav class="fixed top-5 left-10 right-10 z-50 bg-white/95 backdrop-blur-sm rounded-full shadow-sm border border-gray-200 dark:bg-gray-900/95 dark:border-gray-700">
    <div class="max-w-screen-xl mx-auto px-4 py-6">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-2">
          <img src="logo.svg" alt="Logo" class="h-8">
          <span class="text-lg font-semibold">Mi Sitio</span>
        </div>
  
        <div class="hidden md:flex items-center gap-6">
          <a href="#" class="text-gray-600 hover:text-blue-600 dark:text-gray-300 dark:hover:text-blue-400">Inicio</a>
          <a href="#" class="text-gray-600 hover:text-blue-600 dark:text-gray-300 dark:hover:text-blue-400">Servicios</a>
          <a href="#" class="text-gray-600 hover:text-blue-600 dark:text-gray-300 dark:hover:text-blue-400">Contacto</a>
        </div>
  
        <button class="md:hidden">
          <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>
    </div>
  </nav> -->